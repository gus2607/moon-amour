# Copy to .env.local (gitignored) for local dev. In production these go in
# Vercel → Settings → Environment Variables — never commit real values.
# Get them from Supabase → Project Settings → API, after running
# supabase/schema.sql in the SQL editor.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
